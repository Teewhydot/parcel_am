// ========================================================================
// Wallet Domain - Barrel Export
// ========================================================================

const { walletService, WalletService } = require('./wallet-service');

module.exports = {
  walletService,
  WalletService
};
